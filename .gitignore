# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env
.env
.env.local

# Protótipo de referência — material de apoio ao desenvolvimento, não faz parte do app
referencia/

# Claude Code — permissões locais pessoais, não compartilhadas com o time
.claude/settings.local.json

# Supabase CLI — estado local do `supabase link` (project ref, versões), não é segredo
# mas é regenerado por máquina; não precisa versionar
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
